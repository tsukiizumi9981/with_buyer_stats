const STORE_KEY="toulove-v2-orders",DRAFT_KEY="toulove-v2-draft";function getOrders(){try{return JSON.parse(localStorage.getItem(STORE_KEY)||"[]")}catch{return []}}function saveOrders(v){localStorage.setItem(STORE_KEY,JSON.stringify(v))}function money(n){return "NT$"+Number(n||0).toLocaleString()}function uid(){return Date.now().toString(36)+Math.random().toString(36).slice(2,8)}function orderTotal(o){return o.items.reduce((s,x)=>s+x.price*x.qty,0)}function orderUnits(o){return o.items.reduce((s,x)=>s+x.qty,0)}function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[m]))}function setActiveNav(){const file=location.pathname.split("/").pop()||"index.html";document.querySelectorAll("nav a").forEach(a=>a.classList.toggle("active",a.getAttribute("href")===file))}function downloadCSV(filename,rows){const csv="\uFEFF"+rows.map(r=>r.map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(",")).join("\n");const blob=new Blob([csv],{type:"text/csv;charset=utf-8"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=filename;a.click();URL.revokeObjectURL(a.href)}document.addEventListener("DOMContentLoaded",setActiveNav);